import { ComingSoon } from "@/components/ComingSoon";

export default function DocumentsPage() {
  return (
    <ComingSoon
      title="Documents"
      note="Document checklists (required / received / missing) per shipment are part of the next phase."
    />
  );
}
