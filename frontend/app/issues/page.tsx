import { ComingSoon } from "@/components/ComingSoon";

export default function IssuesPage() {
  return (
    <ComingSoon
      title="Issues"
      note="The exception tracker (delays, document problems, customs holds) is part of the next phase."
    />
  );
}
