import { ComingSoon } from "@/components/ComingSoon";

export default function RatesPage() {
  return (
    <ComingSoon
      title="Rate Requests"
      note="Rate-request messages and partner rate entry/comparison live inside each RFQ. Open an RFQ from the RFQ Inbox to generate partner messages, enter received rates, and compare them."
    />
  );
}
